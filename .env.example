# shigindo-flutter-skills の環境変数。
#
# 本ファイルを `.env`（gitignore 済み）にコピーして実際の値を入れる。
# Flutter アプリは通常、ランタイムではなくビルド時に
# `--dart-define=KEY=value` で env を取り込む。各変数はここに記述する —
# `aikata doctor` が `AGENTS.md` または `ARCHITECTURE.md` での
# クロスリファレンスを検証する。

# 例:
# API_BASE_URL=https://api.shigindo-flutter-skills.example
# SENTRY_DSN=
